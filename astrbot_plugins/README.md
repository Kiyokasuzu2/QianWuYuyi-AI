# 浅雾羽依 AstrBot 插件

## 安装步骤
1. 将 `astrbot_plugin_yuyi` 文件夹复制到 AstrBot 的 `data/plugins/` 目录
2. 重启 AstrBot：`docker restart astrbot`
3. 在 QQ 中发送 `/羽依 你好` 测试

## 配置
确保羽依 API 服务已启动，地址与插件中的 `yuyi_api_url` 一致。
默认：`http://localhost:5000/api/chat`
